const categoriesModel = require('./categories');
const productsModel = require('./products');
const drugstoresModel = require('./drugstores');

module.exports = {
  categoriesModel,
  productsModel,
  drugstoresModel,
}